import { GeneralDetailsPipe } from './general-details.pipe';

describe('GeneralDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
