import { AddProductsPipe } from './add-products.pipe';

describe('AddProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new AddProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
